---
name: project_telegram_alert_invalid_token_data
description: SHIPPED (PR #176, merged 2026-08-01) — heal invalid token decimals or auto-stop + Telegram alert; NaN-deposit root cause
metadata: 
  node_type: memory
  type: project
  originSessionId: 78a8529a-26ef-44db-9111-a84bd3dec37f
---

NTH (polish, not a bug): add a Telegram alert when a lifetime scan yields improper on-chain data — e.g. `NaN`/undefined token `decimals` producing a `NaN` lifetime deposit — for rare/illiquid tokens.

**Why:** decimals are cached **only** as a side effect of the price-gated P&L-tracker init (`bot-loop-detect.js:239-248`: the `position.decimals0/1 = ps.decimals0/1` assignment lives inside `if (price0 > 0 || price1 > 0)`). A token with **no price at init** (e.g. prod NFT #71544 = INCENTIVE `0x1Cf43d…` / impostor-"HEX" `0xeD1Fb376…`, both 18 decimals on-chain, minted 2024-06-21) → block skipped → `position.decimals0/1` undefined → `computeDepositUsd` does `10 ** undefined = NaN` → `a0=NaN a1=NaN → $NaN` → `total = NaN || 0 = 0` → `_recordScanSuccess` sets `ready = false` → `lifetimeScanComplete` never true → **perpetual "Syncing" badge + disabled "All Positions Stats" button** (gate needs ALL running positions ready). User's stance: savvy LP provider recognizes flaky memecoin data; edge case, not urgent.

**Status (2026-08-01):** MERGED to `main` via **PR #176** (commit `6e38aab`, merge `5a65a02`). Verified manually on Dev against the exact Prod-failure path (NFT #71544). `npm run check` green: 2518 tests, 93.1% coverage, 0 lint; all main CI/Security-Audit/Pages workflows green. New tests: `test/bot-recorder-heal.test.js`, `test/bot-cycle-drain.test.js`, `test/bot-deposit.test.js`. **Telegram:** DISTINCT new event `positionDataInvalid` ("Position Auto-Stopped: Invalid Token Data", default ON) for the bad-data auto-stop, added to the setup dialog (`_EVENT_IDS` in dashboard-telegram.js + `tgEvt_positionDataInvalid` checkbox in index.html, checked). `positionRetired` stays drain-only ("Drained Position Auto-Retired") and was ALSO added to the dialog — it fired default-ON but was missing from the checkbox list. All EVENT_DEFAULTS keys now appear in the dialog. (Front-end Pool Details manual-decimals override + Force + notices also built; frontend is untested glue like `dashboard-price-override.js`.) **Design (post-conventions-audit — reuses `getPoolState`, no new module):** 3 parts interlock on ONE shared heal step `_ensureTokenDecimals` in `_scanLifetimePoolData` (bot-recorder-lifetime.js):
1. **Heal:** resolve `decimals0/1` through **`getPoolState`** (the codebase's single entry point for on-chain pool/token state — it already validates decimals `[0,77]` via `pool-state-validate.js` + retries RPCs). Decouples decimals from the price-gated init. `_validDecimals` reuses `isIntegerInRange(d,0,77)`. Runs always on full rescan; else only when decimals missing/invalid → short-circuits (no extra RPC) on healthy positions. (An earlier draft added a bespoke `src/token-metadata.js` decimals+symbol resolver — DELETED as a duplication of getPoolState / mirror of its validators. Symbols dropped from scope: they have their own `getTokenSymbol` path.)
2. **Unhealable → auto-stop + Telegram:** "can't heal" = `PoolStateInvalidError` whose `.field` is `decimals0/decimals1` (structural). RPC exhaustion (`PoolStateUnavailableError`) and non-decimals `PoolStateInvalidError` (price/sqrtPrice/tickSpacing) → transient, retry next rescan, NEVER retire (matches pollCycle's pollError tolerance). Structural → set `botState._retireReason` → poll cycle's new `checkRetireRequest` (bot-cycle-drain.js) fires `notify("positionDataInvalid", {reason})` (distinct default-ON dialog event) + returns `{retired:true}` → existing `onRetire` (bot-loop.js:423 → createOnRetire in server-positions.js: status→`stopped`, auto-compound off, `saveConfig`, drop state, `positionMgr.removePosition`).
3. **"Reload Position" reloads ALL data incl. decimals:** already sets `_needsFullRescan=true` + re-fires the scan; the heal's full-rescan branch re-resolves decimals through the SAME code (zero duplication — user mandate). No Reload-specific code needed.
Plus belt-and-suspenders `Number.isFinite` NaN guard in `bot-deposit.js`.

**Workflow constraint (user, 2026-07-31) — SATISFIED 2026-08-01:** reproduced/verified manually on Dev first (NFT #71544: managed-startup scan short-circuited the heal; **Reload Position** exercised the resolve branch → `[token-decimals] decimals OK via chain (d0=18 d1=18)`, deposit intact, `ready=true`), then committed + merged. The Reload-driven drop in "Fees Compounded" was diagnosed as correct mark-to-market re-computation (stale cached `totalCompoundedUsd` wiped by `_clearDiskConfigForKey` + recomputed at current price), not a heal bug — see [[project_current_panel_historical_prices]]. Belt-and-suspenders NaN guard in `bot-deposit.js`. Also see [[feedback_add_good_logs]] — log `d0/d1`. Related: [[project_silent_scan_failure_recurrence]], [[feedback_nice_to_haves_not_bugs]], [[feedback_no_duplicate_code]], [[feedback_go_slowly_means_signoff]].
