{
  "_comment": "Default values for user-editable Bot Config settings AND server-internal operator-tunable defaults. The dashboard reads the top-level keys at init via GET /api/bot-config-defaults and uses them as the initial input value when .bot-config.json has no explicit override. The server also falls back to these when `getConfig` is asked for a value the user hasn't set. Top-level keys match the bot-config-v2 POSITION_KEYS / GLOBAL_KEYS naming so the same string flows from this file \u2192 dashboard input \u2192 .bot-config.json \u2192 bot reader. Nested groups (lowGasThresholds, residualCleanup) are server-internal: not exposed in the Bot Settings UI, consumed directly by src/gas-monitor.js and src/bot-cycle-residual.js. ui-defaults.json, dust-threshold.json and nft-providers.json stay standalone (different concern + data shape).",
  "approvalMultiple": 20,
  "gasFeePct": 1,
  "_gasFeePct_bounds_comment": "Validation bounds for gasFeePct (UI input min/max). Mirror to public/index.html input[min|max] attrs. Consumed by src/swap-gates.js (gasFeePctToRatio clamping) and src/bot-config-defaults.js (_NORMALIZERS.gasFeePct).",
  "gasFeePctMin": 0.1,
  "gasFeePctMax": 15,
  "_priceCacheTtl_comment": "Price-source cache TTL in milliseconds (in-memory cache inside src/price-fetcher.js, gated by the idle-driven pause). Default 120_000 (2 min) \u2014 long enough to absorb the 300 s poll cadence but short enough that a real price move shows up promptly when something's looking. The dust-unit-price cache TTL is computed at module-load as priceCacheTtlMs * dustUnitPriceCacheMultiplier, so the integer-multiple constraint is structural (a runtime assertion guards against future hand-edits).",
  "priceCacheTtlMs": 120000,
  "dustUnitPriceCacheMultiplier": 30,
  "_moveCacheTtl_comment": "Price-source cache TTL while inside a withFreshPricesAllowed scope (rebalance/compound). Default 4_000 ms (4 s) \u2014 short enough that any move-relevant price change shows up but long enough to collapse the rapid-fire burst that the rebalance pipeline triggers when multiple stages (gas gate, slippage estimate, PnL snapshot, etc.) each call fetchTokenPriceUsd for the same token. Without this, a single rebalance can hit each price source 4-6\u00d7 for one token in a few seconds. See src/price-fetcher.js and src/price-fetcher-gate.js#inMove.",
  "moveCacheTtlMs": 4000,
  "_pricePauseExceptionPollWindowMultiple_comment": "Balanced-band Telegram notifier: number of bot poll cycles between fresh-price fetches that bypass the idle-driven price-lookup pause. Effective cadence = checkIntervalSec \u00d7 pricePauseExceptionPollWindowMultiple seconds. Default 10 (10\u00d7 poll = 50 min at the default 300 s poll). Higher = lighter price-source load, slower band-crossing detection. Positive integer >= 1. Only consulted when the 'positionBalanced' Telegram event is enabled.",
  "pricePauseExceptionPollWindowMultiple": 10,
  "rescanPricesDefaultDays": 60,
  "rebalanceOutOfRangeThresholdPercent": 5,
  "rebalanceTimeoutMin": 180,
  "slippagePct": 0.75,
  "checkIntervalSec": 300,
  "minRebalanceIntervalMin": 10,
  "maxRebalancesPerDay": 5,
  "offsetToken0Pct": 50,
  "_rebalanceRangeWidthPct_comment": "Default Price Range Extension (percent of current price) that the 'Default' button on the Price Range Extension row (Bot Settings \u2192 Range & Execution) applies to the input. Positions without a saved override use preserveRange() at rebalance time (existing on-chain tick spread) \u2014 this value is NOT auto-applied to the bot; it's purely the value the button injects into the input so the user can Save it with one click. Per feedback_one_literal_per_shipped_default the literal lives here only. Raised 15 \u2192 80 on 2026-08-05 from operator experience on PulseChain: at 15 percent a volatile pair leaves range quickly, and each rebalance realizes impermanent loss and pays gas. A wider band earns a lower fee rate per unit of liquidity but goes out of range far less often, which in practice has been the better trade in these markets. Bounded 0.1..200 by src/bot-config-defaults.js (200 = the full plus/minus 100 percent span).",
  "rebalanceRangeWidthPct": 80,
  "_fullRangeRebalanceEnabled_comment": "Per-position 'Full-Range Rebalance' toggle. When true, every rebalance mints at MIN_TICK / MAX_TICK via rangeMath.fullRange(), ignoring rebalanceRangeWidthPct. Driven by the 'Full-Range' checkbox next to the Price Range Extension input. Replaces the old rebalanceRangeWidthPct === 100 full-range sentinel.",
  "fullRangeRebalanceEnabled": false,
  "lowGasThresholds": {
    "_comment": "Wallet low-gas detection tunables. Used by src/gas-monitor.js to compute two tiers for the Mission Control 'Gas Running Low' / 'Gas Critical' badge and matching Telegram alerts. Formula (chain-agnostic \u2014 gas opcodes are identical across EVMs): floor_gas_units = standardSendGas \u00d7 worstCaseGasFactor (one worst-case rebalance); recommended_gas_units = floor_gas_units \u00d7 safetyMultiplier \u00d7 managedPositionCount. Native-token threshold = gas_units \u00d7 live gasPrice. Tiers: 'ok' at balance \u2265 recommended; 'low' at floor \u2264 balance < recommended (amber static); 'critical' at balance < floor (blinking \u2014 next rebalance will likely fail with out-of-gas). worstCaseGasFactor=91 was derived from observed PulseChain rebalance_log.json + live logs (NFT #158965\u2192158967) covering a rebalance that fired all 3 corrective-swap iterations: remove(~209k) + primary approve(~46k) + primary swap(~243k) + 3\u00d7corrective(approve+swap ~290k each = ~870k) + mint approves(~76k) + mint(~464k) \u2248 1,911,000 gas / 21,000 send \u2248 91. safetyMultiplier=3 is the user-specified headroom factor. standardSendGas=21000 is the EVM constant for a native-token transfer with no calldata. If the JSON is missing or malformed, gas-monitor falls back to these same defaults so the guard never silently disables.",
    "worstCaseGasFactor": 91,
    "safetyMultiplier": 3,
    "standardSendGas": 21000
  },
  "residualCleanup": {
    "_comment": "Automatic residual-cleanup rebalance tunables. After every successful normal or manual rebalance, if the wallet residual share of the pool batch (residual / (LP + residual)) still exceeds thresholdPct past delayMs, the bot fires exactly one cleanup rebalance to sweep residuals back into the position. The cleanup respects ALL other rebalance rules (throttle, daily cap, doubling, swap-backoff, pause). Back-to-back cleanups are prevented by a per-position flag that only clears on the next normal rebalance. Not exposed in Bot Settings \u2014 operator-tunable via this file only.",
    "delayMs": 600000,
    "thresholdPct": 5
  }
}
